<% if flash[:notice] %>
<h3 style="color: red"><%= flash[:notice] %></h3>
<% end %>



<h3>Employees:</h3>
<% @employees.each do |e| %>
    <li>
        <%= link_to e.first_name + " " + e.last_name, employee_path(e) %>
    </li>
<% end %>
